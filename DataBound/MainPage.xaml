<Page
    x:Class="DataBound.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DataBound"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:DataBound"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="20,20,0,20">
            <TextBlock Name="name" Text="name: " VerticalAlignment="Center" FontSize="20"/>
            <TextBox Name="namebox" Width="150" Margin="20,0,20,0" PlaceholderText="input name"/>
            <Button Name="Add" Content="Add" Click="Add_Click" VerticalAlignment="Center"/>
        </StackPanel>

        <GridView Grid.Row="1" ItemsSource="{x:Bind InputPeople}" IsItemClickEnabled="False">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:People">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{x:Bind Name}" Width="50" HorizontalAlignment="Center" Margin="20,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
